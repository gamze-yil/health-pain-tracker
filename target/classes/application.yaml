spring:
  application:
    name: health-pain-tracker

  datasource:
    url: jdbc:mysql://localhost:3306/health_pain_tracker?useSSL=false&serverTimezone=UTC
    username: root
    password: 31gyfb96.
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8080
